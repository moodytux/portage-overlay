--- Build.PL	2012-11-25 02:44:53.000000000 +0000
+++ Build.PL.new	2014-01-11 17:30:10.491787260 +0000
@@ -28,9 +28,10 @@
     }
 );
 
-my $backend = ! $build->y_n(
-    "Does this system serve as a frontend [yn]?"
-);
+my $backend = 'y';
+#my $backend = ! $build->y_n(
+#    "Does this system serve as a frontend [yn]?"
+#);
 
 $build->feature('backend' => $backend);
 
